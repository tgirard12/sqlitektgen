apply plugin: 'idea'
apply plugin: 'groovy'

repositories {
    mavenCentral()
}

jar.baseName = rootProject.name

dependencies {
    compile gradleApi()
    compile localGroovy()

    testCompile gradleTestKit()
}
